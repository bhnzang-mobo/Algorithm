동적 프로그래밍을 이용하는 방법을 구상해서
구현하려 시도했는데
이중 포문으로 구현해버렸다..
점화식을 세우면 
dp(s,e)가 대칭이면
dp(s-1,e+1)은 arr[s-1] arr[e+1]이 같다면 대칭이다.
이런식으로 s,e가 서로 같은 dp(i,i)를 1에서 N까지
dp(i,i+1)을 1에서 N까지 진행한다.

이 알고리즘을 굳이~~~~재귀함수로 구현하기는 조~금그래서
그냥 이중포문으로 만들었는데 풀렸다.

어쩌라고 풀린걸.

그래서 원리가 아닌거같아 다른 사람들의 풀이를 참고했다.

원리는 비슷했으나 재귀함수로 구현해 구현하는 과정 자체는
점화식으로 구성돼 시인성이 훨씬 높았다.